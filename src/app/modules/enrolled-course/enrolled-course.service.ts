import { TEnrolledCourse } from "./enrolled-course.interface"


const insertEnrolledCourseIntoDB = async (userId: string, enrolledCourse: TEnrolledCourse) => {
    
}

export const EnrolledCourseServices = {
    insertEnrolledCourseIntoDB,
}